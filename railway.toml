[build]
builder = "nixpacks"
buildCommand = "npm run build"

[deploy]
# Flag -s crítica: SPA fallback → cualquier ruta desconocida devuelve
# index.html. Sin ella, /home /ramos /perfil dan 404 en refresh/direct nav.
startCommand = "npx serve -s dist -p $PORT"
